# Plan — Cierre de Phase 0 de ORZAR + higiene del repo

## Contexto

El repo está en **Phase 0 (Discovery)** según `docs/05-roadmap.md`: la documentación (entregable de la fase) ya existe, y hay dos sesiones de trabajo sin commitear (incorporación de docs ORZAR + limpieza legacy + quick wins de la auditoría). El riesgo explícito de la fase es avanzar sin resolver las open questions #1–3. Además se detectó una contradicción activa: el tracker de scraping (pre-ORZAR) viola las reglas adoptadas y puede romper el deploy. Decisiones del usuario ya tomadas: **eliminar el tracker** y **alcance = cerrar Phase 0** (sin código de producto).

## Paso 1 — Eliminar el tracker (alineación con la política adoptada)

Archivos a tocar:
- Borrar `scripts/track-prices.mjs` y `.github/workflows/price-tracker.yml`.
- `docs/24-decision-log.md`: añadir entrada con formato oficial (Decision / Context / Options considered / Chosen / Why / Trade-offs / Date): eliminación del tracker de scraping; snapshots manuales como política vigente hasta resolver OQ #3; opciones consideradas (eliminar / ratificar / pausar).
- Limpiar referencias (grep `track-prices` tras borrar):
  - `AGENTS.md` (lista de `scripts/`),
  - `README.md` (estructura del repo y comandos),
  - `HANDOFF.md` (si lo menciona).
- No se toca `scripts/validate-json.mjs` (sigue exigiendo `source: "manual"`, que es la política vigente).

Por qué: scraping con UA falsa viola AGENTS.md ("No scrape", "no evadir protecciones de terceros"), HANDOFF.md, `docs/09` y `worker/paapi.md`; y escribía `source: "tracker"` que el validador rechaza → rompería el propio workflow y el deploy de Pages (`pages.yml` también corre `validate:data`). El workflow además tenía un bug de bash (`console.log` en línea 43). Nunca llegó a commitear nada (0 commits del bot).

## Paso 2 — Actualizar `CHANGELOG.md`

- Entrada 2026-08-27 con el formato existente (fecha + resumen): incorporación de docs ORZAR + limpieza legacy; quick wins de transparencia de precios/SEO (referencia de descuento = mínimo 30 días, JSON-LD, OG); eliminación del tracker.
- Corregir la afirmación desactualizada "ninguna línea de código de aplicación todavía": aclarar que el sitio Astro + scripts + worker existen como activo de transición pre-ORZAR, y que "Phase 0" se refiere a código de producto de la plataforma nueva.

## Paso 3 — Brief de decisión para OQ #1–3 en `docs/25-open-questions.md`

Añadir a cada pregunta una sección breve "Material para decidir" (mantener `docs/25` como fuente única):
- **#1 Nombre definitivo**: pasos exactos de verificación — OEPM (localizador de marcas: tramites2.oepm.es) y EUIPO eSearch (euipo.europa.eu/eSearch) para "ORZAR"/"ORZA"; disponibilidad de dominios orza.es/orzar.es vía WHOIS (HANDOFF: no comprar todavía). Plantilla de entrada para el decision log cuando el usuario decida.
- **#2 Categorías del catálogo inicial**: resumen de opciones con trade-offs — (a) mantener nicho actual del sitio vivo (descanso + teclados), (b) periféricos PC (recomendación de la spec: densidad de specs comparables), (c) híbrido; impacto en el primer connector de Phase 2 y en el catálogo existente.
- **#3 Estrategia de datos**: opciones de `docs/09` — (a) connector que respete robots.txt/ToS mientras se gana elegibilidad PA-API (gate: 3 ventas cualificadas/180 días), (b) fuente alternativa inicial, (c) esperar + registro manual. Nota: la eliminación del tracker deja el registro manual como política interina vigente; decisión final requiere revisión legal (OQ #6 relacionada).

## Paso 4 — Refresh de snapshots (requiere precios del usuario)

Los últimos snapshots son del 23–24 ago (umbral de frescura 7 días: ~30–31 ago). El usuario comprueba los precios **visualmente en Amazon.es** (el agente no consulta Amazon: acabamos de eliminar el scraper precisamente por eso) y los pasa; entonces:
- `npm run snapshot -- --id <id> --price <n> --date YYYY-MM-DD` por producto.
- Si el usuario no proporciona precios en esta sesión, el paso queda anotado como pendiente inmediato.

## Paso 5 — Commits (explícitamente solicitados vía aprobación de este plan)

Estructura propuesta (commits convencionales, sin atribución IA):
1. `docs: incorporate ORZAR documentation and retire legacy site files` — docs/00–31, CHANGELOG (creación), README/HANDOFF/PRODUCT/DESIGN/AGENTS, borrado de HTML legacy, `docs/estudio-modelos-ia-frontend.html`.
2. `fix(site): align price claims and metadata with ORZAR principles` — quick wins: `src/lib/catalog.ts`, `src/pages/index.astro`, `src/pages/p/[id].astro`, `src/pages/guias/ofertas-semana.astro`, `src/layouts/Base.astro`, `docs/noche/glm52-registro.md`.
3. `chore: remove listing scraper and its cron` — borrado del tracker + entrada en decision log + limpieza de referencias + CHANGELOG.
4. `docs: add decision brief for Phase 0 open questions` — docs/25 (+ snapshots si los hay).

Sin push automático salvo petición (el push dispara el deploy de Pages).

## Verificación

1. `npm run validate:data` → OK.
2. `npm run build` → 24 páginas, verde.
3. `grep -rn "track-prices"` → sin referencias en src/docs/README/AGENTS/workflows.
4. `git status` limpio tras los commits; `git log --oneline -5` coherente.
5. En GitHub (tras push): confirmar que el workflow "Price Tracker Cron" ya no aparece en Actions y que `pages.yml` pasa.

## Fuera de alcance (anotado, no hacer)

- Decisiones OQ #1–3 en sí (las toma el usuario; el agente registra en decision log cuando haya decisión).
- Código de Phase 1+ (NestJS/Next/Postgres): gateado por el roadmap hasta cerrar OQ #1–3.
- Activar `worker/telegram/check.mjs` (requiere bot token + decidir hosting).
- Asset og:image 1200×630, restantes OQ #4–13.
