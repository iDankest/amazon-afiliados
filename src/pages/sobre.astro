---
import Base from '../layouts/Base.astro';

const base = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : import.meta.env.BASE_URL + '/';
---

<Base title="Sobre Orza — Rastreador de Precios y Ofertas">
  <article class="doc">
    <h1 style="margin: 0 0 1rem;">Sobre ORZA</h1>

    <section class="doc-section">
      <p><strong>Orza</strong> es una plataforma de descubrimiento y seguimiento de productos para ayudar a compradores en España a comprar con criterio en <strong>Amazon.es</strong>.</p>
      
      <h3>¿Cómo funciona?</h3>
      <p>Seguimos pocos productos, con el ASIN verificado en Amazon.es, en categorías como periféricos, teclados mecánicos, audio y descanso. Lo que ofrecemos:</p>
      <ul>
        <li><strong>Histórico real, no nuestro:</strong> el gráfico de precios de cada ficha lo sirve <a href="https://keepa.com" rel="nofollow noopener" target="_blank">Keepa</a> con datos de Amazon.es, en el momento de la carga. Orza no almacena, no estima y no calcula precios.</li>
        <li><strong>Criterio, no algoritmo:</strong> elegimos pocos productos y explicamos por qué. Sin tops automáticos ni descuentos inflados.</li>
        <li><strong>Compra directa y transparente:</strong> enlaces con atribución de afiliado a Amazon.es, siempre marcados.</li>
      </ul>

      <h3>Modelo de Sostenibilidad</h3>
      <p>Orza se financia a través del <strong>Programa de Afiliados de Amazon EU</strong> y espacios publicitarios limpios. Cuando compras a través de nuestros enlaces, generamos una pequeña comisión que permite mantener los servidores y la infraestructura de datos sin ningún coste adicional para ti.</p>
    </section>

    <p class="doc-colophon">ORZA · Tracker de precios, chollos y comparador inteligente para Amazon.es</p>
  </article>
</Base>

<style>

  .doc {
    background: var(--card);
    border: 1px solid var(--line);
    border-radius: 14px;
    box-shadow: var(--shadow-subtle);
    padding: 1.6rem 1.6rem 1.75rem;
  }


  .doc h1 {
    margin-bottom: 1rem;
  }

  .brandplate {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    border: 1px dashed var(--line);
    border-radius: 12px;
    padding: 1.1rem 1.25rem;
    margin: 0 0 1.25rem;
    background: var(--card-alt);
  }

  .brandplate-mark {
    width: 64px;
    height: 64px;
    flex-shrink: 0;
  }

  .brandplate-text {
    font-size: 0.9rem;
    color: var(--muted);
    margin: 0;
  }

  .doc-section {
    border-top: 1px solid var(--line-subtle);
    padding-top: 1.1rem;
  }

  .doc-colophon {
    font-size: 0.82rem;
    color: var(--muted);
    border-top: 1px solid var(--line-subtle);
    margin: 1.6rem 0 0;
    padding-top: 0.9rem;
  }

  @media (max-width: 560px) {
    .doc {
      padding: 1.25rem 1.1rem 1.4rem;
    }

    .brandplate {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.85rem;
    }
  }
</style>
