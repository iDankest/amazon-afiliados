# Tag de Amazon Associates (España). Vacío = enlaces a Amazon.es sin `tag=` (sin comisión).
# No pongas aquí claves de PA-API ni secretos: eso va en el host del worker, nunca en el repo.
PUBLIC_AMAZON_TAG=
